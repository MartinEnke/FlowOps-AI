{
  "name": "flowops-ai",
  "version": "1.0.0",
  "main": "dist/server.js",
  "license": "ISC",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js"
  },
  "devDependencies": {
    "prisma": "^7.1.0"
  },
  "dependencies": {
    "@prisma/client": "^7.1.0",
    "dotenv": "^17.2.3"
  }
}
